/** Name of the cookie the shared-token gate issues. Shared by the middleware
 *  (Edge) and the /api/auth route (nodejs), so it lives outside both. */
export const SESSION_COOKIE = "cockpit_session";
